export const metadata = {
  title: "Cart | House Of Kimani Store",
  description: `Cart for e-ecommerce, selling products, and new productivity`,
};

const layout = ({ children }: { children: React.ReactNode }) => {
  return children;
};

export default layout;
